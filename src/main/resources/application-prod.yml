server:
  shutdown: graceful
management:
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
spring:
  lifecycle:
    timeout-per-shutdown-phase: 1m
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mariadb-1-service:3306/trendpick
    username: lldj
    password: lldj123414
  jpa:
    hibernate:
      ddl-auto: create
  data:
    redis:
      host: redis-1-service
